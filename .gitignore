__pycache__/
.venv/
results/
# synthetic data is regenerated deterministically; CIC npz is license-restricted
data/
# trained models ARE artifacts — shipped in the repo (~2MB total);
# only the machine-specific run metadata is excluded
models/meta.json
